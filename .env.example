# Nothing is required to run the panel — it is front-end only and stores every
# score in the browser's localStorage.
#
# When a real server exists, this is the one variable that turns it on. See the
# "Making it real" section of README.md; `src/auth/auth.ts` is the only file
# that needs to change.
VITE_JUDGES_API_URL=
